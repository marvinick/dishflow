<%= simple_form_for [@menu, @food] do |form| %>
  <% if food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food.errors.count, "error") %> prohibited this food from being saved:</h2>

      <ul>
        <% food.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :name, placeholder: "enter name" %>
  </div>
  <br  />
  <div class="field">
    <%= form.text_field :price, placeholder: "enter price" %>
  </div>
  <br  />
  <div class="field">
    <%= form.rich_text_area :body, placeholder: "enter description" %>
  </div>
  <br  />
  <div class="actions">
    <%= form.submit "Submit", class: "btn btn-dark" %>
    
  </div>

<% end %>
